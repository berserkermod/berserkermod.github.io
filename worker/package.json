{
  "name": "berserkermod-api",
  "version": "1.0.0",
  "private": true,
  "description": "BERSERKERMOD production API — licenses, coach backend, Oracle proxy, error ingest (Cloudflare Worker)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
